{
  "name": "about",
  "version": "2.0.0",
  "homepage": "https://prixedox.github.io/about",
  "private": true,
  "scripts": {
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build",
    "build": "mkdir -p dist && cp index.html style.css resumeIcon.png dist/",
    "start": "python3 -m http.server 3000 || npx serve ."
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
